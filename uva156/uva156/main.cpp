#include<iostream>
#include<string>
#include<vector>
#include<map>
#include<set>
#include<stack>
using namespace std;


typedef set<int> Set;
map<Set, int> cnt;
vector<Set> data;


int map_1(Set s)
{
	if (cnt[s])
		return cnt[s];
	else
	{ 
	}
		
}

int main()
{
	string order;
	cin >> order;
	for (int i = 0; i < order.size(); i++)
	{
		switch (order[i])
		{
		case 'P': 
		default:
			break;
		}
	}
}